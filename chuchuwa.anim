chuchuwa:
 compañia 
 -> brazosTendidos
 -> puñosCerrados
 -> dedosArriba

// Todas las animaciones pueden tomar opcionalmente el tiempo que se supone que duren,
// si no se especifica se toma por defecto la suma de lo que tarda cada pedazo de la animación

compañia:
 -> tPose // Estandar, default 0s de duración
 * { rightShoulder: (30°, _, _), leftShoulder: (30°, _, _) } // Sintaxis para especificar nuevos valores

brazosTendidos:
 ->  